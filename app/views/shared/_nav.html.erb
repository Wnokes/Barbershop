<div class="container">
	<div class="row justify-content-md-center nav">
		<div class="col-md-2">
			<h1> <%= link_to("Home", root_path, class: "nav-link link") %></h1>
		</div>
		<div class="col-md-6 center-text polaroid oval logo">
			Wicked Bill
		<div class="body-font subtitle">
			Mobile Barber
		</div>
		</div>
		<div class="col-md-2">
			<h1> <%= link_to("Portfolios", portfolios_path, class: "nav-link link") %></h1>
		</div>
	</div>
	<div class="row justify-content-md-center">
			<% if signed_in? %>
		<div class="col-md-3 center-text card">
				<%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn link" %>
		</div>
			<% end %>		
	</div>
</div>